---
import { getLangFromUrl, useTranslations } from '../i18n/utils';
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const allHorses = await getCollection('portfolio');
const premiumHorses = allHorses.filter(h => h.data.premium && !h.data.sold);
---

<section id="premium" class="bg-bg-subtle relative border-t border-gray-100">
	<!-- THE PREMIUM CONTENT WRAPPER -->
	<div class="relative min-h-[60vh] py-8 md:py-16">
		
		<!-- THE LOCK SCREEN OVERLAY -->
		<div id="premium-lock" class="absolute inset-0 z-[50] flex flex-col items-center justify-start bg-white/95 w-full transition-all duration-700 px-4">
			<!-- STICKY BOX to ensure it stays in view while scrolling the massive overlay on mobile -->
			<div class="sticky top-1/4 mt-10 md:mt-24 max-w-md w-full space-y-6 md:space-y-8 text-center reveal reveal-up bg-white p-8 md:p-14 shadow-2xl border border-gray-100">
				<img src="/images/logo.png" alt="Equivest Premium" class="w-16 h-16 md:w-20 md:h-20 mx-auto object-contain brightness-0 animate-slow-spin" />
				<h2 class="text-3xl md:text-4xl font-serif text-primary">VIP Access</h2>
				<p class="text-primary/70 text-base md:text-lg">Please enter your exclusive access code to view our private collection.</p>
				
				<form id="premium-form" class="mt-8 space-y-4">
					<div>
						<input 
							type="password" 
							id="access-code"
							required
							class="w-full bg-bg-subtle border border-primary/20 p-4 text-center tracking-[0.2em] font-bold text-primary focus:outline-none focus:border-accent transition-colors"
							placeholder="ENTER CODE"
						/>
						<p id="error-msg" class="text-red-500 mt-2 text-sm font-bold uppercase tracking-widest hidden">Invalid Access Code</p>
					</div>
					<button type="submit" class="w-full bg-primary text-white font-bold uppercase tracking-[0.2em] py-4 hover:bg-accent transition-colors">
						Unlock Collection
					</button>
				</form>
				
				<p class="text-xs md:text-sm text-primary/70 mt-6">To request access, please contact your Equivest representative.</p>
			</div>
		</div>

		<!-- THE PREMIUM CONTENT (Blurred by default) -->
		<div id="premium-content" class="px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto opacity-30 select-none pointer-events-none transition-all duration-1000 h-[500px] overflow-hidden">
			<div class="max-w-3xl space-y-4 reveal reveal-up mb-12">
				<span class="text-accent text-xl font-bold uppercase tracking-[0.4em]">Unlocked</span>
				<h2 class="text-4xl md:text-6xl font-serif text-primary leading-tight">
					Private <span class="text-accent italic">Collection</span>
				</h2>
				<p class="text-lg font-medium text-primary/90 leading-relaxed max-w-2xl">
					Welcome to the exclusive environment. These assets are only available to vetted partners and elite clients.
				</p>
			</div>

			{premiumHorses.length === 0 ? (
				<div class="text-center py-10 md:py-20 border border-primary/10 bg-white">
					<p class="text-2xl font-serif text-primary">No exclusive listings available at this moment.</p>
				</div>
			) : (
				<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-2 gap-10">
				{premiumHorses.map((horse, index) => (
					<div class="horse-card reveal bg-white group overflow-hidden border border-primary/10 shadow-premium transition-transform flex flex-col h-full hover:-translate-y-2">
						<div class="aspect-[16/10] overflow-hidden relative">
							{typeof horse.data.image === 'string' ? (
							<img 
								src={horse.data.image} 
								alt={horse.data.title} 
								onerror="this.onerror=null; this.src='/images/logo.png'; this.style.objectFit='contain'; this.style.padding='3rem'; this.style.opacity='0.2';"
								class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
							/>
							) : (
							<Image 
								src={horse.data.image} 
								alt={horse.data.title} 
								class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
							/>
							)}
							{horse.data.youtube_url && (
							<span title="Video Coming Soon" class="absolute inset-0 bg-primary/30 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity cursor-not-allowed">
								<div class="w-16 h-16 bg-white flex items-center justify-center rounded-full text-primary/70 scale-90 group-hover:scale-100 transition-transform">
									<svg class="w-8 h-8 ml-1" fill="currentColor" viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
								</div>
							</span>
							)}
						</div>
						
						<div class="p-8 flex-grow flex flex-col">
							<h3 class="text-3xl font-serif text-primary group-hover:text-accent transition-colors mb-4">{horse.data.title}</h3>
							<p class="text-primary/70 text-lg font-medium mb-8">
							{horse.data.description}
							</p>
							
							<div class="mt-auto grid grid-cols-2 gap-y-6 border-t border-gray-100 pt-6 bg-bg-subtle -mx-8 px-8 pb-8">
							<div class="space-y-1">
								<span class="text-lg uppercase tracking-widest text-primary/60 font-bold block">Age</span>
								<span class="text-xl font-serif text-primary">{horse.data.specs?.age} Years</span>
							</div>
							<div class="space-y-1">
								<span class="text-lg uppercase tracking-widest text-primary/60 font-bold block">Level</span>
								<span class="text-xl font-serif text-primary">{horse.data.specs?.level}</span>
							</div>
							<div class="space-y-1">
								<span class="text-lg uppercase tracking-widest text-primary/60 font-bold block">Gender</span>
								<span class="text-xl font-serif text-primary">{horse.data.specs?.gender}</span>
							</div>
							<div class="space-y-1">
								<span class="text-lg uppercase tracking-widest text-primary/60 font-bold block">Height</span>
								<span class="text-xl font-serif text-primary">{horse.data.specs?.height}</span>
							</div>
							</div>
							
							<div class="mt-4 pt-4 border-t border-primary/10">
								<span class="text-sm uppercase tracking-widest text-primary/50 font-bold mb-2 block">Financial Projection</span>
								<div class="flex items-center justify-between">
									<div>
									<span class="text-[0.65rem] text-primary/70 uppercase block">Acquisition</span>
									<span class="text-xl font-bold text-primary">{horse.data.specs?.purchase_price || '-'}</span>
									</div>
									<div class="text-accent/50 text-2xl">→</div>
									<div class="text-right">
									<span class="text-[0.65rem] text-primary/70 uppercase block">Target Sale</span>
									<span class="text-xl font-bold text-accent">{horse.data.specs?.target_sale || '-'}</span>
									</div>
								</div>
							</div>
							
							<div class="pt-6">
								<a href="https://wa.me/15613010984" target="_blank" class="block w-full text-center py-4 text-xl font-bold uppercase tracking-[0.2em] bg-accent text-white hover:bg-primary transition-all">
									Express Interest
								</a>
							</div>
						</div>
					</div>
				))}
				</div>
			)}
		</div>
	</div>
</section>

<script>
	const VALID_CODES = ['1111'];

  function unlockPortfolio(immediate = false) {
    const lockScreen = document.getElementById('premium-lock');
    const content = document.getElementById('premium-content');

    if (!lockScreen || !content) return;
    
    if (immediate) {
       lockScreen.classList.add('hidden');
    } else {
       lockScreen.classList.add('opacity-0', 'pointer-events-none');
       setTimeout(() => {
         lockScreen.classList.add('hidden');
       }, 700);
    }
    
    content.classList.remove('opacity-10', 'opacity-30', 'pointer-events-none', 'h-[500px]', 'overflow-hidden');
    content.classList.add('opacity-100');

    if (typeof (window as any).initReveal === 'function') {
      (window as any).initReveal();
    }
  }

  function initLock() {
    if (localStorage.getItem('equivest_unlocked') === 'true') {
       unlockPortfolio(true);
       return;
    }

    const form = document.getElementById('premium-form');
    const input = document.getElementById('access-code') as HTMLInputElement;
    const errorMsg = document.getElementById('error-msg');

    if (form && input && errorMsg) {
      // Remove old listeners by cloning to be safe (though on swap it's a new node anyway)
      
      form.addEventListener('submit', (e) => {
        e.preventDefault();
        const code = input.value.trim().toUpperCase();

        if (VALID_CODES.includes(code)) {
          errorMsg.classList.add('hidden');
          localStorage.setItem('equivest_unlocked', 'true');
          unlockPortfolio();
        } else {
          errorMsg.classList.remove('hidden');
          input.classList.add('border-red-500');
          input.value = '';
          form.classList.add('animate-shake');
          setTimeout(() => form.classList.remove('animate-shake'), 500);
        }
      });
    }
  }

  if (typeof initGalleryFilters === 'function') { initGalleryFilters(); }
  initLock();
  
  document.addEventListener('astro:after-swap', () => {
      if (typeof initGalleryFilters === 'function') { initGalleryFilters(); }
      initLock();
  });
</script>

<style>
	.animate-shake {
		animation: shake 0.5s cubic-bezier(.36,.07,.19,.97) both;
	}
	@keyframes shake {
		10%, 90% { transform: translate3d(-1px, 0, 0); }
		20%, 80% { transform: translate3d(2px, 0, 0); }
		30%, 50%, 70% { transform: translate3d(-4px, 0, 0); }
		40%, 60% { transform: translate3d(4px, 0, 0); }
	}
</style>
